async fn connect_db() {
    println!("Hello");
    /* 
    let quote = sqlx::query!("select * from quotes;")
    .fetch_one(&mut conn)
    .await?;

    // anonymous struct has `#[derive(Debug)]` for convenience
    println!("{quote:?}");
    println!("{}: {} -{}", quote.id, quote.quote, quote.author);*/
}